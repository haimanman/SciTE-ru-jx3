# Можно переключаться между файлами сокращений для различных языков с помощью меню Language
# а можно подключить их все вместе
# Например так:
# поскольку в веб-документах часто используются PHP, JS, и т.д.
# Такая возможнось есть только в SciTE-Ru <http://scite-ru.org>

import html.abbrev
import php.abbrev